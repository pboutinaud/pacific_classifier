[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: long-running end-to-end / integration tests (deselect with -m "not slow")
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning:shap.*
